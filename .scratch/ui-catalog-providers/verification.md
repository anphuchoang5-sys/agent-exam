Status: needs-info

# 分层验收与证据规范

> 这是分阶段验收规范。任务 01–04 已完成相应实现与验证；任务 03、04 的执行证据分别由[03 行动](../../docs/actions/2026-09-21-ui-comparison-report.md)和[04 行动](../../docs/actions/2026-09-19-task-04-catalog-candidates-and-scale.md)维护。任务 05 已有独立策略组件、隔离的 `internal_test` 身份切片、S2 固定配置渲染、S6 代理服务与 T1 证据；T2 尝试因侧车退出 127 未测得断言，正式 Worker/Harbor 链、工具循环和生命周期仍未通过；06–08 未实施。未实际运行、失败或跳过的检查不得写成通过。

## 1. 测试层次与通过条件

| 层次 | 适用项 | 要证明什么 | 不能替代什么 |
|---|---|---|---|
| 静态 HTML | 01 | 桌面/手机、角色、状态、点击动线被用户理解并认可 | 产品实现、HTTP授权与真实执行 |
| 长期存储与恢复 | P | 持久卷跨重建/重启、显式 schema 升级和端口隔离；备份恢复已移出课设范围，明确没有灾难恢复保证 | 假提供方安全、真实模型兼容、备份能力或原任务 14 的完整远程验收 |
| HTTP + 领域/适配器测试 | 02–07 | 公开行为、边界、事务前置、幂等、配置拒绝和报告语义 | 真实 PG、浏览器与容器隔离 |
| 隔离真实 PG/MinIO | 04–08及相关02/03变更 | 新旧 schema、事务竞争、快照/制品、恢复、约束迁移 | 用户现有数据库安全或长期部署 |
| 少量完整浏览器 | 02、03、04、06–08 | 两角色真实操作、手机布局、服务器错误反馈和证据访问 | 所有网络/权限负例；它们主要由HTTP覆盖 |
| 新题固定 Fork 离线 | 04 | 每题参考/空/错误补丁的可区分结果，隐藏信息隔离 | 模型能解题或新提供方可用 |
| 假供应商隔离容器 | 05 | 固定CLI完整工具循环、代理/限额/网络/生命周期 | 真实平台账户、真实响应或准确账单 |
| 单题真实 API | 06、07（P、05 完成后） | 获准固定链路可完成工具执行、补丁、独立判卷和清理 | 六题覆盖、整个 MVP 完成 |
| 冻结矩阵 + 全量回归 | 08 | 新范围结果可复查且旧功能无回归 | 原任务14遗留网络验收、长期服务安全 |

只在检查真实执行且输出符合成功标准时记 pass。跳过、依赖不可用、退出码异常、断言错误分别记录；基础设施失败不能冒充“错误补丁正确被拒绝”。

## 2. 需求覆盖表

| 确认来源 | 必须出现的验收 | 归属 |
|---|---|---|
| Q1–Q4 | 桌面/手机全操作、角色首页、向导保留选择、浅色重点清楚、用户原型确认 | 01–03 |
| Q5 | 五个新增固定候选分别通过参考/空/错误判卷，旧题仍可用 | 04 |
| Q6、Q8被Q9替代 | 保留指定 DS alias映射与 Kimi K3；只用开放平台原生协议，拒绝订阅/错误端点 | 06、07 |
| Q7 | 1/4/6/9/20题和1/3配置合法；0/21题、0/4配置、重复/停用/未知项拒绝；最多60 Runs | 04 |
| Q10 B | 长期Key不进做题容器、令牌受Run/模型/期限/额度约束；绕过及跨Run负例 | 05 |
| Q11 | 账户地区匹配、预算假设复核、假值探针/真实调用分开、所有自动重试为0 | 05–08 |
| Q12 | 同题跨配置矩阵、来源可查、未知非零、限制差异显式、单次详情保留 | 03、08 |
| Q13 | 仅本机私有文件管Key；Web/API/DB无秘密；读取权限失败关闭 | 05–07 |
| Q14 | 每项停点、无自动推进、暂停恢复不重跑旧Job/不挪用历史授权 | 每项收尾 |
| 现有回归 | owner自提交自批、越权、内部测试隔离、批准/取消竞争、旧快照、恢复不续跑、新Job重试、排行/制品保留 | 02–08 |

## 3. 可执行检查入口

以下命令须在对应实施项获安排、工具及依赖已核对后才运行；不是规划阶段待执行命令。执行前先读项目脚本和重型测试显式开关。路径相对指定工作目录。

后端工作目录 `E:\9.1agent_exam\apps\backend`：

```powershell
.venv/Scripts/ruff.exe check src tests prototype_codex_harbor_e2e.py
.venv/Scripts/ruff.exe format --check src tests prototype_codex_harbor_e2e.py
.venv/Scripts/mypy.exe src prototype_codex_harbor_e2e.py
.venv/Scripts/python.exe -m pytest tests/catalog tests/jobs -q -p no:cacheprovider --tb=short
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider --tb=short
```

第一次定向失败用例选择当前任务的测试文件；最终全量命令不能代替显式重型检查。默认 pytest 跳过的真实存储/容器用例逐项列 skipped，不能算通过。

Web 工作目录 `E:\9.1agent_exam\apps\web`：

```powershell
npm run typecheck
npm run lint
npm run build
npm run test:e2e
```

浏览器运行器采用现有合成账号/隔离后端。先读其实际参数和权限要求；不得盲目访问正在运行的用户实例。新增移动端用例明确 viewport、actor、操作与截图证据。

重型检查入口分别为 [目录验证脚本](../../apps/backend/tests/catalog/runtime/verify.ps1)、[Job验证脚本](../../apps/backend/tests/jobs/runtime/verify.ps1)、[Fork集成](../../apps/backend/tests/integration/test_swe_bench_integration.py)、[Harbor网络实测](../../apps/backend/tests/integration/test_harbor_network.py)。运行前读取完整参数/显式开关，选当项需要的专属 scope；不猜连接串，不照抄历史私有命令。新增代理检查 CLI 必须在05实现时记录准确命令、参数与资源标签；本轮没有虚构可运行入口。

## 4. 必须补齐的负例

### UI、目录与 Job

- 未登录、协作者冒充owner、读他人Job/Run/制品、internal_test 混入正式列表/排行榜；UI隐藏按钮不代替服务器拒绝。
- 空/失败/慢请求、连续点击、返回上步、变更配置、超时重试、刷新链接、会话过期、分页边界与失效筛选。普通步骤切换不清选择；重新登录不得恢复其他用户的私有选择。
- 新快照写入后读出摘要一致；旧样本按旧算法读回；新策略不能改变原Job快照；同幂等键不同请求冲突；创建事务失败不留半个矩阵。
- 比较矩阵中 resolved=false、infra_error、incomplete、missing要分开；null不能被加总成0；真实美元费用以外不填cost_usd；制品失效与轨迹缺失保留可理解提示。

### 代理与限额

- 未批准、错误profile、缺密钥、宽权限/链接文件、错误模型/路径/Host、任意Authorization、URL/重定向、额外远程工具：出站计数0且脱敏错误。
- 跨Run令牌、过期/撤销令牌、同令牌重放超额、客户端改TOML/CLI/env、直连供应商/宿主/metadata/其他Trial、IPv6/DNS旁路：不能扩大已授权限。
- 使用假Key探查容器文件、环境、进程、日志、Docker inspect、patch、公开报告/制品和错误；代理只能在最小可信内存范围持有，禁止正文日志。假值探查也是秘密保护测试，不复制任何真实Key。
- 原子额度预留、恰好临界/超一单位、多轮累计输入含反复历史、输出含推理、并发请求、断流/未知usage、429/5xx、CLI重连、进程重启/状态丢失：不静默增额/退回未知用量/重试。
- 正常结束、错误、模型超时、协作取消、外层超时、代理/Worker崩溃：当前Run访问按合同收束，旧Job不续跑，新Job重试重新批准；精确清理可证，失败资源单列。

预算首值、数学推导与账号限制唯一见[研究第4–5节](../../docs/research/2026-09-17-codex-provider-config-and-budget.md)。05技术上验证不了保守输入上界、输出约束和中断结算时，不能承诺硬金额上限，也不能放行完整真实矩阵。

## 5. 证据格式与收尾

每轮新建任务专属、Git忽略的证据作用域，不覆盖旧scope。行动记录保存安全摘要与命令，至少含：

- 当轮基准/工作区差异、任务编号、测试case、命令工作目录、依赖/数据/镜像身份。
- 环境类型（合成/真实PG/真实API）、运行次数和重试次数、预期/实际/退出码、失败分类、跳过原因。
- Job/Run非秘密关联、提供方请求/返回身份、配置摘要、每题判卷结果与缺失用量标记；有账单观察才记实际费用。
- 测试专属容器/网络/卷/临时进程的创建与结束核对；只清理明确属于scope的资源，保留缓存、framework/runtime旧证据和用户已有服务。
- 文档、测试、评审、剩余风险及下次停点；Standards/Spec固定基准、修复与复验结果。

Key、令牌、真实账号/设备/私有网络地址、私有文件绝对路径、原始供应商正文不写Git。公开证据继续通过现有清洗与制品边界；原始材料仅owner私有保存，既有例外不能扩展为共享许可。

任务完成需“验收项全有结论且必过项通过、文档一致、两轴评审已处理、清理结果明确”，不能以提交数量、单次模型通过或官方教程存在代替。运行任务14负向/VPN/离线检查不是本计划自动授权；它的遗留项在独立行动维护。

## 6. 任务 03 实际证据（2026-09-21）

- Web：38 项隔离浏览器回归、类型检查和默认生产构建通过；03 的 6 项浏览器用例覆盖真实批次、混合五档、按配置指标、同一 Job 多配置、权限、手机、轨迹、下载、并发和异步竞态。
- 后端：默认全量 415 项通过、81 项按显式外部环境门禁跳过；报告定向 16 项通过、2 项跳过；专属 PostgreSQL+MinIO Job 门禁 175 项通过并精确清理测试资源。
- 静态：Ruff check/format、Mypy 通过；双轴审查发现的问题已修复并增加回归，最终 Standards/Spec 均 PASS。没有新增 API、数据库表、Key 页面或模型调用，也没有写共享业务库。
- 完整命令、环境失败分类和警告见[03 行动](../../docs/actions/2026-09-21-ui-comparison-report.md)，本文件不复制临时路径和日志。

# Harbor 与题目框架适配核验

## 状态与情况说明

- 状态：已取代（Superseded）
- 日期：2026-09-01
- 来源：用户要求科普 Harbor 的架构、说明本项目如何利用 Harbor，并确认 Harbor 是否完全符合题目指定的六项框架。
- 核验基线：用户在当前对话中给出的评测框架、Docker 沙箱、统一 Runner、LLM-as-Judge 与人工抽检、Next.js 15 + React 19、PostgreSQL + MinIO 六项要求。
- 当前阶段：研究与架构讨论；不编写业务代码，不创建或定稿项目架构文档。
- 已确认：Harbor 可作为候选底层评测引擎；是否最终采用及采用范围仍待用户确认。
- 核心未知：Harbor 对 FAIL_TO_PASS、stdin→stdout 补丁协议、网络管控、人工抽检、指定前端和指定存储的原生覆盖程度。
- 取代原因：用户指出题目同时明确引用 SWE-Gym，需要先确认 SWE-Gym 在题目中的真实角色；在完成该核验前，不能继续以 Harbor 为主引擎展开适配设计。

## 实施措施

1. 仅使用 Harbor 官方仓库、官方文档、规范和源代码核验能力，不根据名称或印象猜测接口。
2. 拆解 Harbor 的 Task、Dataset、Agent、Environment、Trial、Job、Verifier、Trajectory 等核心模块及数据流。
3. 将六项题目要求逐条标记为“原生满足、需要适配、必须自研、存在冲突”，并给出证据和风险。
4. 设计 Harbor 与本平台之间的候选接口和 Adapter 位置，但不把候选方案写成已定架构。
5. 把来源化结论追加到既有相似项目研究记录，避免建立重复事实源。

## 需要修改的文件树

```text
E:\9.1实训\docs\
├── actions\2026-09-01-harbor-fit-analysis.md
│   └── 本次 Harbor 适配核验的范围、措施、文件变化和验证结果。
└── research\2026-09-01-agent-evaluation-platforms.md
    └── 追加 Harbor 与题目六项框架的逐项事实核验和适配结论。
```

设计关系：研究记录继续作为外部框架事实的唯一真实源；行动记录只描述本次如何核验。候选架构中的 Harbor 接入将使用 Adapter 模式隔离 Harbor 版本和命令变化，但本轮不创建实现文件。

## 修改后自验证方式

1. 检查六项题目要求均有明确结论、官方依据和缺口说明。
2. 检查 Harbor 内部概念与本平台候选术语没有混用。
3. 检查“Harbor 已有能力”和“本项目建议”明确分开。
4. 检查未创建业务代码、`ARCHITECTURE.md` 或定稿领域词汇表。
5. 检查研究记录中的链接均指向 Harbor 官方仓库、官方文档或官方规范。

## 自验证情况

- 已停止 Harbor 主引擎方向的进一步核验，未据此创建业务代码或定稿架构文档。
- 已检查既有研究记录：后台核验在停止前没有追加未完成的 Harbor 适配章节。
- 本记录保留为被取代的研究过程证据，不代表 Harbor 已被排除，也不代表 Harbor 已被选定。

# 行动：任务 05 下一轮 T2 的准备（诊断 + 纠正一条走不通的路径）

> 状态：**已完成**（2026-09-22）。纯文档与仓库内证据；未在负责人机器上执行任何操作，未改探针断言。

## 1. 情况说明

**来源**：用户 2026-09-22 要求"下一步做什么直接说"。本轮先做只读核对，结果发现**附三 给出的下一轮做法在代码上走不通**，必须纠正后才值得占用负责人的下一个窗口。

**已核实的事实**（本轮实际读码与实测，不采信转述）：

| 项 | 结论 | 证据 |
|---|---|---|
| 产品入口是否允许 T2 所需的拓扑 | ❌ **不允许**：`extra_docker_compose` 会被判为非法配置 | `harbor_entry.py:136-143`（`any(environment.get(key) for key in (..., "extra_docker_compose", ...))` → `HARBOR_NETWORK_CONFIG_INVALID`） |
| 该门禁是否有意为之 | ✅ 是，且有测试钉住 | `tests/contract/test_execution_network.py:190-198` 的 `extra` 篡改用例要求抛 `HARBOR_NETWORK*` |
| 侧车适配的正确接法 | ✅ 仓库内已有先例两行 | `tests/codex_trial_probe.py:36-38`：`export_sidecar(repo/"framework/harbor", context, HARBOR_REVISION)` + `DockerEnvironment._EGRESS_CONTROL_SIDECAR_CONTEXT_PATH = context` |
| 适配为什么能避开 127 | ✅ 机制清楚：`export_sidecar` 用 `git show <rev>:<path>` 从 **git 对象**导出（`network.py:129-139`），因此恒为 LF，并注入 M0 已授权的 DNS 守卫 | `network.py:110-139`；侧车源文件为 `Dockerfile`/`entrypoint.sh`/`gost.yaml`/`allowlist.txt`/`bin/network-policy`（`network.py:21-27`） |
| CRLF 是否会真的造成 127 | ✅ 本仓库可实证：我们自己的探针脚本就是索引 LF、工作树 CRLF | `git ls-files --eol apps/backend/tests/providers/runtime/` → `topology-probe.sh`、`topology-lib.sh` 均为 `i/lf w/crlf` |
| 首轮 127 的原始证据是否已取到 | ✅ 已取到并已入库（负责人只读写证） | [`docs/actions/2026-09-22-task05-sidecar-127-diagnosis.md`](2026-09-22-task05-sidecar-127-diagnosis.md)：`[FATAL tini (7)] exec /opt/egress-sidecar/entrypoint.sh failed: No such file or directory`、`Exited (127)`、镜像本机构建（`RepoDigests` 空）；**原 `probe.py` 未设 `_EGRESS_CONTROL_SIDECAR_CONTEXT_PATH`、未调用 `export_sidecar()`**；该记录明确"仅凭这四条证据不能区分镜像内文件缺失 / 解释器不可用 / 换行"，**根因尚未定位** |
| 受控拒绝与零出站能否在本机复核 | ✅ 已实测（含真实流式应答） | 见第 5 节；`tests/providers/lifecycle/serve_proxy.py` |

**已确认的结论**：下一轮 T2 **不能经产品入口**（那里是为生产收紧的门禁，不该为试验放宽），只能走**探针 + 显式携带适配**这条分支；而 附三 目前把这条写成"备选"。这是本轮唯一需要纠正的路径问题——其余（授权范围、最小形态、回报要求）不变。附四 已按此改写，并把诊断收敛为**一条**只读命令（首轮原始证据已由负责人取证入库，不需要再取一遍）。

## 2. 实施措施

1. 记录两项发现：产品入口拒绝 `extra_docker_compose`（走不通）；侧车适配的正确接法与机制（127 的最可能成因）。
2. 给出**只读诊断**命令（不创建任何资源，60 秒内可判断 CRLF 假设是否成立）。
3. 把 附三 的路径纠正写进[组长机器预案](2026-09-21-task05-owner-machine-runbook.md)（新增 附四，原 附三 不删改，保留当时判断）。
4. 把结论写进任务单 Comments 与[进度日志](../LLY/03-progress/PROGRESS_LOG.md)。
5. 本机侧顺带完成：把工作树里那份未提交的手工代理脚本定稿并入库（它是唯一不需要 Docker 就能看到真实受控拒绝与真实流式应答的入口，正好给 Web 侧的呈现核对与 S11 集成层做对照）。

**完成标准**：下一轮 T2 的执行路径在代码层可自查（命令与文件行号齐全）；不会再一次因路径本身而空跑一个窗口。

## 3. 受影响文件树

| 路径 | 改动与职责 |
|---|---|
| `apps/backend/tests/providers/lifecycle/serve_proxy.py` | **改并入库**（原为未跟踪）：手工运行的真实代理；本轮修正示例请求体、实测三种情形后提交（`2f96dae`）。 |
| `docs/actions/2026-09-21-task05-owner-machine-runbook.md` | **改**：新增 附四（诊断命令 + 路径纠正 + 回报要求）；原 附一/二/三 原样保留。 |
| `.scratch/ui-catalog-providers/issues/05-fake-provider-secure-execution-chain.md` | **改**（仅 Comments）：记录发现与纠正后的下一轮做法。 |
| `docs/LLY/03-progress/PROGRESS_LOG.md` | **改**：当日条目追加一条。 |
| `docs/actions/2026-09-22-t05-t2-next-round.md` | 本文件（新增）。 |

未改动：任何探针断言与 verdict 文件、`harbor_entry.py`、`request_policy.py`、任务单正文与验收项。

## 4. 自验证方式

| 检查 | 手段 | 期望 |
|---|---|---|
| 断言①：产品入口拒绝拓扑 | 引 `harbor_entry.py:136-143` 与钉住它的测试用例 | 结论可自查 |
| 断言②：适配接法有先例 | 引 `tests/codex_trial_probe.py:36-38` | 可照抄 |
| 断言③：CRLF 风险真实存在 | `git ls-files --eol` 结果 | 本仓库脚本确实 `i/lf w/crlf` |
| 断言④：受控拒绝可本机复核 | 运行 `serve_proxy.py` 并逐条 curl | 403 / 400 / 200 事件流，上游仅 1 条记录 |
| 静态检查 | `ruff check` / `ruff format --check` 对改动的脚本 | 通过 |

## 5. 自验证情况

**已执行**：

- `git ls-files --eol apps/backend/tests/providers/runtime/` → `topology-probe.sh`、`topology-lib.sh` 为 `i/lf w/crlf`（索引 LF、工作树 CRLF），证明"工作树上下文可能带 CRLF"在本仓库是**实际存在**的，不是猜测。
- `ruff check` 与 `ruff format --check` 对 `serve_proxy.py`：通过。
- `serve_proxy.py` 三种情形**实测**（本机，端口 18125）：无凭据 → `403 PROVIDER_ACCESS_DENIED`；持令牌但绑定外模型 → `400 PROVIDER_REQUEST_REJECTED`；持令牌且绑定模型 → `200 text/event-stream` 且以 `response.completed` 收尾；假上游记录**恰好 1 条**，两次拒绝**零条**（"零出站"从外部可见）。
- 修正该脚本原有的一处文档缺陷：原示例请求体为 `"input":[]`，而策略在出站前即以 `REQUEST_INPUT_EMPTY` 拒绝（`request_policy.py:104-112`），所以文档里那条"真实应答"示例只可能得到拒绝。

**未执行与限制（如实记录）**：

- **未在负责人机器上执行任何命令**：本机没有 `framework/harbor`（`.gitignore` 排除），Docker 守护进程也未运行（`docker version` 连接失败）。诊断命令与下一轮做法**只写不跑**。
- **未编写 T2 的"仅断言"脚本**：T1 探针会自建容器，而 T2 的断言必须在 Harbor 建好的 Trial 容器内执行，需要另写一份不建容器、只做判定的脚本，并配最小 job config。**这是下一个可开工切片**，本轮只把它写进计划，未动代码。
- 未改动探针断言与 verdict（负责人已逐行核对过的那份）。

## 6. 下一步（本机可做，不依赖任何人）

1. 写 `tests/providers/runtime/` 下的 **T2 仅断言脚本 + 最小 job config + 驱动**：驱动照 `codex_trial_probe.py:36-38` 携带侧车适配，拓扑按 附三 的最小形态（显式 `internal`/`egress` + 受控 `proxy`/`fake-upstream`），断言沿用 T1 的判定标准但**不创建容器**。
2. 本机无法执行它（无 Harbor、无 Docker 守护进程）；交付形态是"负责人机器上一条命令 + 明确标注未在本机运行"。
3. 负责人机器上的顺序：先跑 附四 的三条只读诊断 → 再跑该驱动 → 按 附三 的回报要求回填。

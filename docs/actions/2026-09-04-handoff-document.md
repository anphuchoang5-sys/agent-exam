# 架构讨论交接文档行动记录

## 状态与情况说明

- 状态：已完成；Handoff 已包含下一窗口的分级必读文档清单。
- 来源请求：用户要求编写 Handoff，说明原任务是在讨论并持续更新架构文档时因工作区路径问题中断，并提醒现有架构及接口文档可能落后于完整对话。
- 当前事实：项目已迁移到 `E:\9.1agent_exam`；迁移完整性已经验证；仓库仍有多份已修改或未跟踪文档，尚未提交。
- 目标：为从新路径启动的下一条 Codex 任务提供可执行的上下文恢复入口，明确可信事实、待对账文档、未决问题和安全边界。
- 未知：现有全部专题文档与本轮长对话之间的差异尚未完成逐项审计，因此 Handoff 不得宣称文档已经完全同步。
- 排除范围：本次不实现业务代码、不解决尚未确认的架构问题、不提交或推送 Git、不读取任何真实凭据。

## 实施措施

1. 检查当前 Git 状态、架构总览和本轮新增的专题/行动文档。
2. 创建根目录 `HANDOFF.md`，记录恢复任务所需的事实、用户已确认决定、可能滞后的文档和推荐下一步。
3. 检查 Handoff 中的路径、链接、Git 状态描述及“已确认/待核验/待确认”措辞。
4. 运行 Markdown 链接目标检查、旧活动路径搜索和 `git diff --check`，把实际结果写回本行动记录。
5. 根据用户反馈，逐项写明“对话已经确认的增量事实”“需要修改的文件和章节”“目标表述”和“不得误写成已验证的内容”，供下一条任务直接执行。
6. 增加下一窗口的阅读顺序、每份文档的用途、应关注内容和已知滞后提示，避免只读总架构后直接行动。

## 对话已经确认的增量事实

下表不是候选方案，而是本次中断前后用户已经确认、需要同步到权威文档的事实：

| 编号 | 已确认内容 | 同步时必须保留的边界 |
|---|---|---|
| F-01 | 首个真实原型 Agent 是 Codex，首批使用 `SWE-Gym/SWE-Gym-Lite` 的 1～3 道真实任务 | 只确认 Agent 和数据集范围；CLI 版本、模型 ID、数据 revision/split/实例仍未固定 |
| F-02 | 首个 Codex 原型使用评测机所有者本人通过 ChatGPT Pro 登录产生的 `auth.json` | 这是认证政策已确认，不代表 Harbor 容器内登录、Token 刷新或真实 Trial 已验证 |
| F-03 | 用户这一台笔电是当前唯一正式真实评测节点；协作者可以开发完整项目，但默认不在各自机器运行真实 Codex Trial | 协作者若自行运行，必须使用自己的 ChatGPT 登录或自己获授权的 API Key |
| F-04 | `auth.json` 不得共享、提交 Git、写入 PostgreSQL/MinIO，或进入日志、轨迹和制品 | 文档只能保存环境变量名、占位路径和脱敏信息，不能记录真实本机凭据路径值或文件内容 |
| F-05 | Kimi 和 DeepSeek API 以后只能作为独立 Agent Configuration 接入 | 一次 Trial 内不得从 OpenAI/Codex 静默切换到 Kimi 或 DeepSeek；换提供方必须产生独立配置和运行证据 |
| F-06 | 工作区已迁移到 `E:\9.1agent_exam`，迁移后默认沙箱的 `setup refresh had errors` 未复现 | 这只证明当前工作区命令启动恢复，不能据此宣称容器内 Codex、Harbor 或 SWE-Bench-Fork 已跑通 |

## 后续架构文档对账的精确修改表

下一条任务应按表执行。这里记录的是修改目标，不代表这些修改已经在目标文件中完成。

| 目标文件与章节 | 当前问题 | 必须改成的具体内容 | 完成后怎样验证 |
|---|---|---|---|
| `docs/architecture/ARCHITECTURE.md` `1 从哪里开始读 | 导航没有 Codex 认证事实源 | 增加一行：想了解 Codex 认证、凭据所有权与协作边界时，唯一维护文档为 `../interfaces/CODEX_AUTHENTICATION.md` | 导航中只出现一个认证权威入口，链接存在 |
| `ARCHITECTURE.md` `3 已确认决定 | 决定表只到 C-22，没有记录认证和正式评测节点 | 新增决定：首个 Codex 原型使用评测机所有者的 ChatGPT Pro `auth.json`；凭据禁止共享和持久化。再新增决定：当前只有一台正式评测节点，真实 Codex Trial 由该机器所有者触发，协作者默认不需要真实凭据 | 能检出 `auth.json`、唯一正式评测节点、协作者不取得所有者凭据；同时仍写明运行待实测 |
| `ARCHITECTURE.md` `4/`5 边界与数据流 | 图和文字没有说明凭据走哪条边界 | 不把 `auth.json` 画成业务数据；在边界规则中写明：FastAPI、PostgreSQL、MinIO 和公开 Job 请求不接收凭据内容；只有执行节点在启动受控 Codex Trial 时从本机秘密配置解析凭据引用 | 数据流中不存在“Web/API 上传 auth.json”；MinIO 制品列表明确排除凭据 |
| `ARCHITECTURE.md` `8 候选文件树 | 接口文档树没有 `CODEX_AUTHENTICATION.md` | 在 `docs/interfaces` 下增加该文件，并标注它是“Codex 认证方式、凭据所有权和秘密边界的唯一事实源” | 规划树路径与真实文件一致 |
| `ARCHITECTURE.md` `10/`11 风险与验证 | 没有独立记录个人凭据泄露风险和原型安全门槛 | 风险表加入：临时 Trial、日志/轨迹/制品可能泄露凭据；控制措施为最小注入、脱敏、排除收集、成功/失败/超时均清理。验证策略加入：首次真实 Trial 前后人工检查容器、可写层、日志、MinIO 和数据库均无凭据 | 风险与验证各有对应条目；不能写成“已通过” |
| `ARCHITECTURE.md` `12/`13 讨论队列与变更记录 | 第 1 项仍要求确认 API Key 或认证文件，已经过期 | 删除该已回答问题；把“固定 Codex CLI 版本、模型 ID、端点白名单，并实测 Token 刷新/清理”保留为技术待办。变更记录写入 F-02～F-05 | 不再出现“认证方式待确认”，但仍能检出版本/模型/运行待实测 |
| `docs/dependencies/DEPENDENCIES.md` `2/`9 | Codex CLI 行把“认证方式”和“版本/模型/运行”一起写成未固定 | 改为：认证政策已经固定为评测机所有者 ChatGPT Pro `auth.json`；Codex CLI 精确版本、模型 ID、端点和容器兼容性仍待固定/实测。待锁定项只保留后者，不把真实凭据当依赖资产 | 依赖表不再说认证选择未知；文件中没有真实路径或凭据 |
| `docs/interfaces/HARBOR_EXECUTION.md` 文档头、`3、`5.1、`6、`13 | 没有引用认证事实源；原型章节仍说认证方式待定 | 文档头链接 `CODEX_AUTHENTICATION.md`。输入边界写明公共请求/数据库只保存非秘密的认证类型，不保存文件内容或真实路径；执行节点从本机 `CODEX_AUTH_JSON_PATH` 读取所有者凭据并只交给受控 Codex Trial。输出/制品明确排除 `auth.json`。`13 改成“认证选择已确认，版本、模型、Token 刷新、脱敏和清理待实测” | 搜索能看到权威链接、`CODEX_AUTH_JSON_PATH` 和排除规则；不能声称真实 Trial 已通过 |
| `docs/interfaces/FRAMEWORK_INTERFACES.md` `2、`7、`15 | 仍把认证方式写成待固定，并把 2026-09-01 Codex CLI 启动失败写成当前限制 | 区分三件事：官方 CLI 接口已核验；认证政策已确认；宿主 CLI 和 Harbor 容器运行仍需分别验证。先在新路径重新执行 `codex --version`、`codex exec --help`，再把旧失败降为历史记录或保留为仍可复现的问题；不得只因沙箱恢复就判定 CLI Adapter 可用 | 记录实际命令、退出码和版本；容器 E2E 继续保持“未验证” |
| `docs/interfaces/CODEX_AUTHENTICATION.md` `2～`6 | 内容基本反映最新对话，但尚未与其他权威文档闭环 | 保持为认证唯一事实源；复核 F-02～F-05 全部存在，并补充其他文档的反向链接。不要增加真实 `auth.json` 路径、内容或 Token | 常见密钥模式扫描为 0；链接存在；“已确认”和“待实测”分开 |
| `docs/actions/2026-09-04-codex-authentication-policy.md` 全文 | 文件树漏列实际新增/修改文档，自验证仍是旧沙箱失败状态 | 文件树改为实际涉及的 ARCHITECTURE、DEPENDENCIES、HARBOR_EXECUTION、FRAMEWORK_INTERFACES、CODEX_AUTHENTICATION 和本行动文件；在完成上述同步后重跑链接、术语、密钥模式与 `git diff --check`，记录真实结果再标记完成 | 行动文档不再留“待执行”，修改树与 `git status` 一致 |
| 根目录 `AGENTS.md` | 文件内容与用户最新声明冲突 | 这不是架构内容，不在对账时擅自修改；先单独询问用户希望删除、清空还是重写 | 未获得选择前不改变该文件 |

## 不得在这轮同步中误写为已确认

| 仍待核验或决定 | 当前正确状态 |
|---|---|
| Codex CLI 固定版本与模型 ID | 待固定 |
| Harbor 容器内使用 `auth.json` 的 Token 刷新、脱敏和销毁 | 待真实原型验证 |
| SWE-Gym-Lite revision、split、首批 `instance_id` 和镜像 digest | 待读取真实元数据后固定 |
| Harbor 本机安装、patch 提取、资源限制和 Trial E2E | 尚未运行 |
| Worker 在宿主运行还是挂载 Docker Socket 的容器 | 待原型裁决 |
| Judge/工具调用/token/耗时是否计入总分 | 待用户决定 |

## 受影响文件树

```text
E:\9.1agent_exam\
├── HANDOFF.md
│   # 新增：下一条 Codex 任务恢复架构讨论与文档对账的唯一交接入口
└── docs\actions\
    └── 2026-09-04-handoff-document.md
        # 新增：本次 Handoff 创建过程与验证证据
```

本次只增加文档，不改变系统模块、接口或设计模式。

## 自验证方式

- 核对 `HANDOFF.md` 是否明确区分：已确认事实、疑似滞后内容、待确认事项和禁止直接假定的内容。
- 验证 Handoff 引用的本地 Markdown 文件均存在。
- 搜索活动文档中的旧工作区路径，历史行动记录除外。
- 运行 `git diff --check`，预期无空白错误。
- 运行 `git status --short`，确认只新增预期文件且保留此前修改。
- 核对 Handoff 的必读表是否覆盖总架构、模块契约、数据模型、关键接口、依赖、本机环境、ADR、术语和本轮行动记录，并确保每个链接目标存在。

## 自验证结果

- 通过：`HANDOFF.md` 已创建，包含项目目标、通俗流程、已确认架构决定、已验证本机事实、已知文档滞后表、未决问题、恢复顺序和安全红线。
- 通过：已补充 6 条对话确认的增量事实、12 条文件/章节级修改要求和 6 条不得误写为已确认的边界。
- 通过：Handoff 引用的 8 个本地 Markdown 目标全部存在，缺失链接数为 0。
- 通过：Markdown 代码围栏共 6 个、数量成对；尾随空白数为 0；滞后点表头存在且唯一。
- 通过：Handoff 与认证专题文档的常见明文密钥模式扫描结果为 0；本次没有读取真实凭据。
- 通过：活动架构、接口、依赖、运维和根级上下文文件中的旧工作区路径匹配数为 0；Handoff 和历史行动记录保留旧路径仅用于说明迁移历史。
- 通过：`git diff --check` 退出码为 0；只出现现有 Markdown 文件未来可能由 LF 转为 CRLF 的提示，没有空白错误。
- 通过：`git status --short` 显示 `HANDOFF.md` 和本行动记录为新增文件，同时完整保留创建前已有的 4 个修改文件及其他未跟踪文档。
- 限制：本次只建立交接和列出滞后点，没有擅自修改仍需对账的架构、依赖或接口事实；下一条任务需按 Handoff 第 6 节逐项同步。
- 通过：Handoff 第 2 节已分为三层阅读清单；第一层列出 11 份开始修改前必须完整阅读的文档，并逐项注明需要了解的事实和已知滞后风险。
- 通过：第二层按模块、数据、Runner、HTTP、Lite 原型、Codex 原型和路径迁移场景提供按需阅读入口；第三层说明研究、历史行动记录和第三方源码的使用边界。
- 通过：Handoff 共解析出 19 个唯一相对链接，目标全部存在，缺失数为 0。
- 通过：更新后 Handoff 代码围栏仍为 6 个、尾随空白为 0，`git diff --check` 退出码为 0；只出现既有 LF→CRLF 提示。

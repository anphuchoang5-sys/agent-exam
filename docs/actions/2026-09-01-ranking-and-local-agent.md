# 排行对象与本地自研 Agent 确认行动记录

## 状态和情况说明

- 状态：已完成
- 来源请求：用户接受排行榜比较“Agent + 模型 + 配置”的完整组合，并询问是否可以测试本地自研 Agent。
- 已确认决定：排行榜的可比对象是完整 Agent 配置，不把不同模型或关键配置混在同一个 Agent 名称下。
- 已有决定：第一版只运行项目组预先适配、审核和固定版本的已登记 Agent。
- 事实解释：本地自研 Agent 可以成为已登记 Agent；它需要通过统一 Runner interface 或专用 Adapter 接入，不能绕过沙箱和证据记录。
- 当前阶段：架构讨论设计，只更新文档，不实现本地 Agent Adapter。
- 明确排除：本次不确定 Runner JSON 字段、本地代码装载方式、首批 Agent 清单和评分权重。

## 实施措施

1. 已在架构约束中记录排行榜单位为完整 Agent 配置。
2. 已在 Agent 登记章节补充本地自研 Agent 的两种合规接入方式和运行限制。
3. 已更新评分章节和待确认问题队列，移除已经确认的排行榜问题。
4. 已更新变更记录并核对术语、Runner seam 和安全规则没有冲突。

完成标准：架构文档能够明确回答“可以测试本地自研 Agent”，同时不会被误解为允许任意本地代码不受限制地执行。

## 需要修改的文件树

```text
E:\9.1实训\docs\
├─ actions\2026-09-01-ranking-and-local-agent.md
│  # 本次两个架构结论的范围、措施和验证证据
└─ architecture\ARCHITECTURE.md
   # 当前架构唯一事实源；同步排行榜单位和本地自研 Agent 接入规则
```

设计关系：本地自研 Agent 与 Codex、Claude Code、Aider 一样位于 `Agent Runner` seam 后面；若遵守统一进程协议则使用 `custom_process.py` Adapter，若接口特殊则增加专用 Adapter。排行榜始终引用 `Agent Configuration`，不直接按 Adapter 名称聚合。

## 修改后自验证方式

1. 检查已确认约束新增完整 Agent 配置排行榜决定。
2. 检查文档明确写出本地自研 Agent 可测试、必须登记、必须固定版本、必须进入沙箱和证据链。
3. 检查排行榜问题已从待确认队列移除。
4. 检查 Judge 和过程指标计分问题仍保持待确认。
5. 检查未创建业务源代码。

## 自验证情况

- 已确认约束检查：新增 `C-09`，明确排行榜比较“Agent + 模型 + 关键配置”的完整组合。
- 本地自研 Agent 检查：架构文档明确写出“已确认可以测试”，并列出统一进程 Adapter 与专用 Adapter 两种候选接入方式。
- 安全与追溯检查：本地自研 Agent 仍须人工登记、固定代码/模型/配置版本、进入 Agent 生成沙箱，并产生统一轨迹、日志、补丁和验证证据。
- 接口纪律检查：目录挂载、代码快照或预构建镜像尚未核验，仍标为待确认，没有伪造最终接口。
- 排行榜检查：旧的排行榜单位问题已从待确认队列移除；页面允许按 Agent 家族展示，但原始成绩不能混合。
- 未决问题检查：LLM Judge 和工具调用/Token/耗时是否进入总分仍保持待确认。
- 范围检查：本次只修改架构文档和本行动记录，没有创建业务代码或安装依赖。
